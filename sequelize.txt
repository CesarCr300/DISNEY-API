*For create a table:
sequelize = const sequelize = new Sequelize({
    dialect: 'sqlite',
    storage: './database.sqlite'
})
I have to create the "connection", in this case is the variable sequelize.
Now, I have to create the model with the table
const nameModel = sequelize.define(nameTable, { 
    attribute: Sequelize.TYPE (INTEGER, STRING, TEXT)
})

If I wanna add validations, when I create the table :
const nameModel = sequelize.define(nameTable, { 
    attribute: {
        type: Sequelize.TYPE,
        allowNull: false,
        validate: {
            notNull:{
                msg:"Este campo no puede ser nulo"
            },
            max: {
                args:5,
                msg: "No puede ser mayor que 5"
            }
        }
    }
})